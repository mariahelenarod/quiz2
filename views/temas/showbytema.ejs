	
<div class="container" style="max-width: 660px">

	<div class="panel panel-default table-responsive" id="showbytema-panel">
		<div class="panel-heading">Tema: <%= quizes[0].tema %></div>
			<table class="table">
				<tbody>
					<% var i; for (i=0; i < quizes.length; i++) { %>
					<tr>
						<td><a href="../quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %> </a></td>
						<% if(session.user){%>
							<td><a href="../quizes/<%= quizes[i].id %>/edit"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a></td>
							<td>
								<form method="post" action="../quizes/<%= quizes[i].id %>?_method=delete">
									<button class="btn btn-danger btn-sm" type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
										<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
									</button>
								</form>
							</td>
						<%}%>
					</tr>
					<% } %>
				</tbody>
			</table>
		</div>
		<p>
			<a href="/temas"><span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span></a>
		</p>
	</div>
	
<div>